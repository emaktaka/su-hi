{
  "name": "numerology-api",
  "private": true,
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "dependencies": {
    "openai": "^5.12.0"
  },
  "scripts": {
    "dev": "vercel dev",
    "build": "echo \"No build step required for API only\"",
    "start": "vercel dev"
  }
}
